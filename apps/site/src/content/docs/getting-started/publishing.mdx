---
title: Publishing to npm
description: Manual versioning and publishing for @thaparoyal/calendar packages
---

# Publishing to npm

Patro now uses a **manual npm release flow**. GitHub Actions no longer publishes packages.

## Packages

| Package | npm | Description |
|---------|-----|-------------|
| `@thaparoyal/calendar-core` | [npm](https://www.npmjs.com/package/@thaparoyal/calendar-core) | Date conversion, themes, shared logic |
| `@thaparoyal/calendar-react` | [npm](https://www.npmjs.com/package/@thaparoyal/calendar-react) | React components and hooks |
| `@thaparoyal/calendar-vue` | [npm](https://www.npmjs.com/package/@thaparoyal/calendar-vue) | Vue composables |
| `@thaparoyal/calendar-svelte` | [npm](https://www.npmjs.com/package/@thaparoyal/calendar-svelte) | Svelte stores |
| `@thaparoyal/calendar-vanilla` | [npm](https://www.npmjs.com/package/@thaparoyal/calendar-vanilla) | Vanilla JS and CDN build |
| `@thaparoyal/calendar-cli` | [npm](https://www.npmjs.com/package/@thaparoyal/calendar-cli) | CLI tooling |

---

## Manual Release Steps

### 1) Create a changeset

```bash
pnpm changeset
```

Commit the generated `.changeset/*.md` file with your code changes.

### 2) Bump versions and changelogs

```bash
pnpm version-packages
```

### 3) Build and test

```bash
pnpm build
pnpm test
```

### 4) Publish to npm

```bash
npm login
pnpm release
```

`pnpm release` runs build + `changeset publish` and publishes updated packages.

### 5) Push version commits and tags

```bash
git push --follow-tags
```

---

## Notes

- Do not use plain `pnpm publish` from the monorepo root.
- Packages are linked in `.changeset/config.json`, so they version together.
- Keep repository visibility public if packages are public on npm.
